from fasthtml.common import *

app, rt = fast_app()

@rt("/")
def get():
    return Main(
        Strong("ข้อความตัวหนา"), Br(),    # ตัวหนา
        Em("ข้อความตัวเอียง"), Br(),      # ตัวเอียง
        U("ข้อความขีดเส้นใต้"), Br(),     # ขีดเส้นใต้
        S("ข้อความขีดฆ่า"), Br(),         # ขีดฆ่า
        Code("โค้ดตัวอย่าง"), Br(),       # โค้ด
        Q("คำพูดตัวอย่าง"), Br(),         # เครื่องหมายคำพูด
        Blockquote("ข้อความบล็อกคำพูด")  # บล็อกคำพูด
    )

serve()